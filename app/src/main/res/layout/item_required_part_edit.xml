<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="4dp"
    app:cardCornerRadius="6dp"
    app:strokeWidth="1dp"
    app:strokeColor="@color/dividerColor"
    app:cardElevation="0dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <TextView
            android:id="@+id/reqPartName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Dell Inspiron Battery"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@color/textPrimary"
            android:layout_alignParentStart="true"
            android:layout_toStartOf="@id/reqPartRemoveBtn" />

        <TextView
            android:id="@+id/reqPartQty"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Required Qty: 1"
            android:textSize="12sp"
            android:textColor="@color/textSecondary"
            android:layout_below="@id/reqPartName"
            android:layout_marginTop="2dp" />

        <ImageView
            android:id="@+id/reqPartRemoveBtn"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:contentDescription="Remove part requirement"
            app:tint="@color/errorColor" />

    </RelativeLayout>

</com.google.android.material.card.MaterialCardView>
