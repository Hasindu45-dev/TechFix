<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions required for Location Services, Maps, and Camera -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TechFix">
        
        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Authentication Activities -->
        <activity 
            android:name=".authentication.LoginActivity" 
            android:exported="false" 
            android:windowSoftInputMode="adjustResize" />
        <activity 
            android:name=".authentication.RegisterActivity" 
            android:exported="false" 
            android:windowSoftInputMode="adjustResize" />

        <!-- Role Dashboards -->
        <activity 
            android:name=".customer.CustomerDashboardActivity" 
            android:exported="false" />
        <activity 
            android:name=".technician.TechnicianDashboardActivity" 
            android:exported="false" />
        <activity 
            android:name=".admin.AdminDashboardActivity" 
            android:exported="false" />

        <!-- Maps Activity -->
        <activity 
            android:name=".maps.BranchLocatorActivity" 
            android:exported="false" />

        <!-- Booking Activity -->
        <activity 
            android:name=".customer.BookAppointmentActivity" 
            android:exported="false" />

        <!-- Repair Operations Activities -->
        <activity 
            android:name=".customer.RepairHistoryActivity" 
            android:exported="false" />
        <activity 
            android:name=".customer.TrackRepairActivity" 
            android:exported="false" />
        <activity 
            android:name=".customer.PaymentActivity" 
            android:exported="false" />

        <!-- Technician Job Detail Activity -->
        <activity 
            android:name=".technician.TechnicianJobDetailActivity" 
            android:exported="false" />

    </application>




</manifest>